import DB from "../../repos/DB";
import { Req, Res, Next } from "../../../lib/types";

export default class CategoryController {
    // list all Categories
    static list(req: Req, res: Res): void {
        //
    }
}
